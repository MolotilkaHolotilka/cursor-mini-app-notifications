# Настройка HTTPS в Coolify

## Способ 1: Автоматический SSL (РЕКОМЕНДУЕТСЯ) ✅

Coolify автоматически выдает SSL сертификаты через Let's Encrypt для ваших доменов.

### Шаг 1: Проверьте настройки домена

1. В настройках приложения найдите раздел **"Domains"** или **"FQDNs"**
2. Убедитесь что домен указан **БЕЗ** `http://` или `https://`
3. Должно быть просто: `q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io`

### Шаг 2: Coolify автоматически выдаст SSL

1. Подождите 1-2 минуты после сохранения домена
2. Coolify автоматически создаст SSL сертификат
3. Проверьте в браузере: откройте `https://your-domain`
4. Должен быть зеленый замочек (SSL работает)

### Шаг 3: Проверьте что SSL активен

В настройках домена должна появиться отметка что SSL выдан и активен.

---

## Способ 2: Если SSL не выдается автоматически

### Вариант А: Использовать свой домен

1. Настройте свой домен (например: `notifications.yourdomain.com`)
2. Добавьте A-запись DNS на IP вашего сервера Coolify
3. В Coolify добавьте ваш домен
4. Coolify автоматически выдаст SSL для вашего домена

### Вариант Б: Проверить настройки SSL

1. В настройках домена найдите раздел **"SSL"** или **"TLS"**
2. Убедитесь что **"Let's Encrypt"** включен
3. Нажмите **"Generate Certificate"** или **"Enable SSL"**

---

## Способ 3: Для sslip.io домена

`sslip.io` домены иногда требуют специальной настройки:

1. Убедитесь что домен указан **БЕЗ** `http://`
2. Должно быть: `q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io`
3. Подождите несколько минут
4. Попробуйте открыть `https://your-domain` в браузере

---

## Проверка HTTPS:

### В браузере:
1. Откройте `https://q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io`
2. Должна открыться страница БЕЗ ошибки SSL
3. В адресной строке должен быть замочек 🔒

### Для Telegram:
После того как HTTPS заработает, отправьте BotFather:
```
https://q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io
```

(с `https://` в начале!)

---

## Если HTTPS не работает:

1. **Подождите 2-5 минут** - Let's Encrypt выдает сертификаты не мгновенно
2. **Проверьте настройки домена** - должен быть без `http://`
3. **Перезапустите приложение** - иногда помогает
4. **Проверьте логи** - нет ли ошибок SSL в логах Coolify

---

## Частая проблема:

Если в настройках домена указано `http://q84oskg0cs044ogwkok0os04...`, Coolify может не выдать SSL.

**Исправление:** Уберите `http://` и оставьте только домен:
```
q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io
```

---

## После настройки HTTPS:

1. ✅ Проверьте в браузере - должен работать `https://`
2. ✅ Отправьте HTTPS URL в BotFather
3. ✅ Mini App в Telegram заработает!

---

**Готово! Попробуйте открыть `https://your-domain` в браузере через 1-2 минуты! 🚀**

