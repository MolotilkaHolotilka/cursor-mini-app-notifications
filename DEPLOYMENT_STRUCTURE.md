# 🏗️ Структура деплоя: Что где хостится

## Вариант 1: Раздельное хостирование (Рекомендуется)

### 📍 GitHub Pages (Фронтенд)
**Что хостится:**
- ✅ `index.html`
- ✅ `app.js`
- ✅ `styles.css`
- ✅ Другие статические файлы (изображения и т.д.)

**Как работает:**
- GitHub Pages берет файлы из вашего репозитория
- Автоматически хостит их на `https://molotilkaholotilka.github.io/cursor-mini-app-notifications/`
- **Тот же репозиторий**, но GitHub Pages берет только HTML/CSS/JS

**Настройка:**
- Settings → Pages → Source: `main` branch → `/` (root)
- GitHub автоматически ищет `index.html` в корне

---

### 📍 Coolify (Backend API)
**Что хостится:**
- ✅ Весь репозиторий (подключаете GitHub)
- ✅ Но Dockerfile выбирает что использовать:
  - `main.py` (FastAPI сервер)
  - `routers/` (API endpoints)
  - `database.py`, `models.py`, `services.py`
  - `requirements.txt` (зависимости)
  - `static/` (опционально, для отдачи фронтенда)

**Как работает:**
1. Coolify клонирует ваш GitHub репозиторий
2. Запускает `Dockerfile`, который:
   - Устанавливает зависимости из `requirements.txt`
   - Копирует Python файлы (API код)
   - Копирует `static/` (если нужно)
   - Запускает `main.py` (FastAPI сервер)
3. FastAPI работает на порту 8000
4. Обрабатывает запросы: `/api/notifications`, `/api/stats` и т.д.

**Что НЕ нужно на Coolify:**
- ❌ `index.html` (корневой) - он на GitHub Pages
- ❌ `app.js` (корневой) - он на GitHub Pages
- ❌ `styles.css` (корневой) - он на GitHub Pages

**Но используется:**
- ✅ `static/index.html` - если хотите отдавать фронтенд через FastAPI
- ✅ `static/app.js` - если хотите отдавать фронтенд через FastAPI

---

## Вариант 2: Все на Coolify (Один домен)

### 📍 Coolify (Frontend + Backend)
**Что хостится:**
- ✅ Весь репозиторий
- ✅ FastAPI отдает:
  - Статические файлы (`static/index.html`, `static/app.js`, `static/styles.css`)
  - API endpoints (`/api/...`)

**Как работает:**
1. Пользователь открывает `https://your-coolify-domain.com/`
2. FastAPI отдает `static/index.html`
3. `index.html` загружает `app.js`
4. `app.js` делает запросы к `/api/...` (тот же домен!)

**Преимущества:**
- ✅ Один домен - нет CORS проблем
- ✅ Проще настройка
- ✅ Не нужно обновлять URL API в коде

**Недостатки:**
- ❌ Нужно каждый раз деплоить при изменении фронтенда
- ❌ Больше нагрузка на сервер (отдает и статику, и API)

---

## Что происходит с репозиторием?

### GitHub Pages автоматически:
```
Репозиторий → GitHub Pages
├── index.html ✅ отдается
├── app.js ✅ отдается
├── styles.css ✅ отдается
├── static/ ✅ (может использоваться, но обычно корневые файлы)
└── Python файлы ❌ игнорируются (GitHub Pages не запускает Python)
```

### Coolify автоматически:
```
Репозиторий → Dockerfile → Контейнер
├── index.html ❌ не используется (если фронтенд на GitHub Pages)
├── app.js ❌ не используется (если фронтенд на GitHub Pages)
├── main.py ✅ используется (FastAPI сервер)
├── routers/ ✅ используется (API)
├── database.py ✅ используется
├── requirements.txt ✅ используется (установка зависимостей)
└── static/ ✅ используется (если хотите отдавать фронтенд)
```

---

## Как Dockerfile выбирает файлы?

Смотрите `Dockerfile`:

```dockerfile
# Копируем весь код приложения
COPY main.py .           # ← Backend
COPY database.py .       # ← Backend
COPY routers/ ./routers/ # ← Backend API
COPY static/ ./static/   # ← Frontend (опционально)
```

Dockerfile копирует только то, что нужно для работы FastAPI.

---

## Рекомендация для вашего случая

### Используйте Вариант 1:
- **GitHub Pages:** Фронтенд (быстро, бесплатно, просто обновлять)
- **Coolify:** Только API (backend)

### Шаги:
1. **GitHub Pages:**
   - Репозиторий уже подключен
   - Просто обновляете HTML/CSS/JS файлы в репозитории
   - GitHub Pages автоматически обновляет сайт

2. **Coolify:**
   - Подключаете тот же GitHub репозиторий
   - Dockerfile автоматически соберет только нужное для API
   - FastAPI работает, обрабатывает запросы от фронтенда

### Структура запросов:
```
Пользователь → GitHub Pages (index.html + app.js)
                    ↓
                app.js делает fetch()
                    ↓
            Coolify API (https://your-api.com/api/notifications)
                    ↓
            FastAPI обрабатывает запрос
                    ↓
            Возвращает данные из БД
```

---

## FAQ

**Q: Нужно ли дублировать файлы?**  
A: Нет! Один репозиторий, но разные хостинги используют разные части:
- GitHub Pages: HTML/CSS/JS из корня
- Coolify: Python код из корня

**Q: Что если изменю код?**  
A: 
- Изменили `app.js` → `git push` → GitHub Pages обновится автоматически
- Изменили `main.py` → `git push` → Coolify пересоберет контейнер (или автодеплой, если настроен)

**Q: Можно ли использовать `static/` на GitHub Pages?**  
A: Да, можно. GitHub Pages может отдавать файлы из `static/`, но обычно используют корневые файлы.

**Q: Нужно ли что-то специально настраивать?**  
A: Нет! Dockerfile уже настроен правильно. Просто подключите репозиторий в Coolify.

---

## Итоговая схема

```
┌─────────────────────────────────────────┐
│     Ваш GitHub репозиторий              │
│                                          │
│  ├── index.html                         │
│  ├── app.js                             │
│  ├── styles.css                         │
│  ├── main.py                            │
│  ├── routers/                           │
│  ├── database.py                         │
│  └── ...                                 │
└─────────────────────────────────────────┘
           │              │
           │              │
           ↓              ↓
┌─────────────────┐  ┌──────────────────┐
│  GitHub Pages   │  │     Coolify      │
│                 │  │                  │
│  index.html     │  │  main.py         │
│  app.js         │  │  routers/       │
│  styles.css      │  │  database.py    │
│                 │  │                  │
│  Фронтенд       │  │  Backend API     │
└─────────────────┘  └──────────────────┘
```

**Один репозиторий → два хостинга → разные части кода!** 🚀

