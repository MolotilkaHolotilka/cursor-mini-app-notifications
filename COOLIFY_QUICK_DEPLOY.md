# 🚀 Быстрый деплой на Coolify

## Подготовка (если еще не сделано)

1. **Закоммить все изменения:**
   ```bash
   git add .
   git commit -m "Ready for Coolify deployment with full API"
   git push
   ```

## Шаги деплоя в Coolify

### 1. Создание проекта в Coolify

1. Зайдите в Coolify Dashboard
2. Нажмите **"New Project"**
3. Введите название проекта (например: `notifications-api`)
4. Нажмите **"Create"**

### 2. Добавление приложения

1. В созданном проекте нажмите **"New Resource"** или **"+ Add"**
2. Выберите **"Git Repository"**
3. Подключите ваш GitHub репозиторий:
   - Нажмите **"Connect GitHub"** (если еще не подключен)
   - Выберите ваш репозиторий: `cursor-mini-app-notifications`
   - Branch: `main` (или ваша основная ветка)

### 3. Настройка сборки

В настройках приложения укажите:

- **Build Pack:** `Dockerfile`
- **Dockerfile Path:** `./Dockerfile`
- **Port:** `8000`
- **Build Command:** (оставить пустым, используется из Dockerfile)

### 4. Переменные окружения

В разделе **"Environment Variables"** добавьте:

```env
# База данных (SQLite - по умолчанию)
DATABASE_URL=sqlite+aiosqlite:///./notifications.db

# Сервер
HOST=0.0.0.0
PORT=8000

# CORS - добавьте домен GitHub Pages и Telegram
ALLOWED_ORIGINS=https://molotilkaholotilka.github.io,https://web.telegram.org

# Опционально: для будущих интеграций
# TELEGRAM_BOT_TOKEN=your_token_here
# AIRTABLE_API_KEY=your_key_here
```

### 5. Volumes (для сохранения БД)

В разделе **"Volumes"** добавьте:

- **Path in Container:** `/app/notifications.db`
- **Path on Host:** (оставить пустым, Coolify автоматически создаст)

Это нужно для сохранения базы данных SQLite между перезапусками.

### 6. Домен

1. В разделе **"Domain"** выберите:
   - Использовать домен Coolify (например: `notifications-api.your-coolify-domain.com`)
   - Или добавьте свой кастомный домен

2. Coolify автоматически настроит HTTPS (Let's Encrypt)

### 7. Деплой

1. Нажмите **"Deploy"** или **"Save & Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. Проверьте логи на наличие ошибок

## Проверка работы

После успешного деплоя проверьте:

### 1. Health Check
Откройте в браузере:
```
https://your-coolify-domain.com/health
```

Должен вернуться:
```json
{
  "status": "ok",
  "message": "App is running"
}
```

### 2. API Documentation
Откройте Swagger UI:
```
https://your-coolify-domain.com/docs
```

### 3. API Endpoints
Проверьте работу API:
```
https://your-coolify-domain.com/api/stats
https://your-coolify-domain.com/api/notifications
```

### 4. Frontend
Откройте главную страницу:
```
https://your-coolify-domain.com/
```

## Настройка фронтенда на GitHub Pages

После деплоя API на Coolify:

### Вариант 1: API на отдельном домене (рекомендуется)

1. Откройте `app.js` (или `static/app.js`)
2. Найдите строку:
   ```javascript
   return 'https://your-coolify-api-domain.com/api';
   ```
3. Замените на реальный URL вашего API:
   ```javascript
   return 'https://notifications-api.your-coolify-domain.com/api';
   ```
4. Закоммитьте и запушьте изменения:
   ```bash
   git add app.js static/app.js
   git commit -m "Update API URL for Coolify"
   git push
   ```

### Вариант 2: Фронтенд тоже на Coolify (все в одном месте)

Если хотите, чтобы фронтенд и API были на одном домене:

1. В Coolify настройте тот же домен
2. FastAPI уже настроен для обслуживания статики
3. `app.js` автоматически использует относительный путь `/api`

## Обновление CORS для GitHub Pages

Убедитесь, что в `main.py` добавлен домен GitHub Pages:

```python
allowed_origins = [
    "https://molotilkaholotilka.github.io",  # ✅ Уже добавлен
    "https://web.telegram.org",
    # ...
]
```

Если нужно добавить еще домены, обновите `main.py` и перезапустите деплой.

## Troubleshooting

### Ошибка 502 Bad Gateway

**Причины:**
- Неправильный порт (должен быть `8000`)
- Приложение не запускается (проверьте логи)

**Решение:**
1. Проверьте настройки порта в Coolify → Settings → Port → `8000`
2. Проверьте логи в Coolify → Logs
3. Убедитесь, что приложение запускается локально

### База данных не сохраняется

**Решение:**
1. Убедитесь, что Volume настроен: `/app/notifications.db`
2. Перезапустите приложение после настройки Volume

### CORS ошибки

**Решение:**
1. Проверьте `ALLOWED_ORIGINS` в переменных окружения
2. Убедитесь, что домен GitHub Pages добавлен
3. Перезапустите приложение после изменения переменных

### Ошибки импорта модулей

**Решение:**
1. Убедитесь, что `requirements.txt` содержит все зависимости
2. Проверьте, что все файлы скопированы в Dockerfile
3. Проверьте логи сборки в Coolify

## Структура после деплоя

```
GitHub Pages (Frontend):
https://molotilkaholotilka.github.io/cursor-mini-app-notifications/

Coolify (API):
https://your-coolify-domain.com/
├── /                    → Frontend (index.html)
├── /api/notifications   → API endpoints
├── /api/stats           → Statistics
├── /api/webhooks        → Webhooks
└── /docs                → API documentation
```

## Чеклист

- [ ] Репозиторий подключен в Coolify
- [ ] Dockerfile настроен
- [ ] Port установлен: `8000`
- [ ] Переменные окружения добавлены
- [ ] Volume для БД настроен
- [ ] Домен настроен
- [ ] Деплой успешен
- [ ] Health check проходит
- [ ] API доступен
- [ ] Frontend обновлен с правильным API URL (если отдельный фронтенд)
- [ ] CORS настроен

Готово! 🎉




