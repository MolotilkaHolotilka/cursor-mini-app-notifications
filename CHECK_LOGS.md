# Как проверить логи приложения в Coolify

## Важно:
Логи которые вы показали - это логи **деплоя**, а не самого приложения!

## Нужно проверить логи **работающего контейнера**:

### Шаг 1: Откройте логи контейнера

В Coolify:
1. Откройте ваше приложение
2. Найдите вкладку **"Logs"** или **"Container Logs"**
3. Откройте её

### Шаг 2: Что искать в логах

**Должны быть строки:**
```
INFO:     Started server process [1]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**Если есть ошибки:**
- `ModuleNotFoundError` - проблема с зависимостями
- `FileNotFoundError` - проблема с путями к файлам
- `Address already in use` - порт занят

---

## Если логи пустые или нет запуска uvicorn:

### Проверка 1: Правильный CMD в Dockerfile

В Dockerfile должно быть:
```dockerfile
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Проверка 2: Файл main.py существует

В контейнере должен быть файл `/app/main.py`

### Проверка 3: Статические файлы скопированы

В контейнере должна быть папка `/app/static/`

---

## Быстрый тест:

Если можете зайти в контейнер через Coolify:

```bash
# Проверить что файлы на месте
ls -la /app/

# Проверить что main.py есть
cat /app/main.py

# Проверить что static/ есть
ls -la /app/static/

# Попробовать запустить вручную
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

## Что мне нужно от вас:

1. Откройте **Logs** контейнера (не логи деплоя!)
2. Скопируйте последние 50-100 строк
3. Отправьте сюда

Или хотя бы скажите:
- Есть ли там строки про "Uvicorn running"?
- Есть ли какие-то ошибки?
- Пустые ли логи?

---

## Альтернатива: Проверить через браузер

Откройте в браузере:
- `http://q84oskg0cs044ogwkok0os04.91.107.212.137.sslip.io`
- Или с https если настроен SSL

Что видите?
- Bad Gateway?
- Пустую страницу?
- Ошибку?

---

**Скопируйте логи контейнера (не деплоя!) и отправьте сюда! 🔍**




