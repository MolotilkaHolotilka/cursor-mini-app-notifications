# Чеклист для деплоя на Coolify

## ✅ Что уже готово

- [x] Dockerfile создан
- [x] .dockerignore настроен
- [x] main.py обновлен для статики
- [x] app.js обновлен с автоматическим определением API URL
- [x] Статические файлы скопированы в static/

## 📋 Шаги деплоя на Coolify

### 1. Подготовка репозитория

1. **Закоммить все изменения:**
   ```bash
   git add .
   git commit -m "Ready for Coolify deployment"
   git push
   ```

2. **Проверить что в репозитории есть:**
   - ✅ Dockerfile
   - ✅ requirements.txt
   - ✅ main.py
   - ✅ static/ (с index.html, styles.css, app.js)
   - ✅ Все остальные файлы проекта

### 2. Настройка в Coolify

1. **Создать новый проект:**
   - Зайдите в Coolify
   - Нажмите "New Project"
   - Дайте название проекту

2. **Добавить новое приложение:**
   - Выберите "New Resource"
   - Выберите "Docker Image" или "Git Repository"
   
3. **Если через Git:**
   - Подключите ваш репозиторий
   - Branch: `main` или `master`
   - Build Pack: `Dockerfile`
   - Dockerfile Path: `./Dockerfile`
   - Port: `8000`

4. **Переменные окружения:**
   ```
   DATABASE_URL=sqlite+aiosqlite:///./notifications.db
   HOST=0.0.0.0
   PORT=8000
   ```
   
   **Опционально (для продакшена):**
   ```
   ALLOWED_ORIGINS=https://your-domain.com,https://web.telegram.org
   TELEGRAM_BOT_TOKEN=your_token
   AIRTABLE_API_KEY=your_key
   ```

5. **Volumes (для SQLite):**
   - Путь в контейнере: `/app/notifications.db`
   - Это сохранит базу данных между перезапусками

6. **Домен:**
   - Настройте домен в настройках приложения
   - Coolify автоматически выдаст SSL сертификат

### 3. Проверка после деплоя

1. **Health Check:**
   ```
   https://your-domain.com/health
   ```
   Должен вернуть: `{"status":"healthy",...}`

2. **API Docs:**
   ```
   https://your-domain.com/docs
   ```
   Swagger UI должен открыться

3. **Frontend:**
   ```
   https://your-domain.com/
   ```
   Должен показать интерфейс уведомлений

4. **API:**
   ```
   https://your-domain.com/api/notifications
   ```
   Должен вернуть список уведомлений (может быть пустым)

### 4. Обновление Telegram бота

После успешного деплоя:

1. Откройте @BotFather в Telegram
2. `/mybots` → выберите вашего бота
3. Bot Settings → Menu Button
4. Configure → Edit Web App
5. Укажите URL: `https://your-domain.com`
6. Сохраните

### 5. Тестирование Mini App

1. Откройте бота в Telegram
2. Нажмите на кнопку с Mini App
3. Проверьте что интерфейс загружается
4. Проверьте что API работает

---

## 🔧 Troubleshooting

### Приложение не запускается

**Проверьте логи в Coolify:**
- Откройте Logs в интерфейсе Coolify
- Ищите ошибки импорта или запуска

**Возможные проблемы:**
- Не установлены зависимости → проверьте requirements.txt
- Ошибка в коде → проверьте логи
- Порт не открыт → проверьте что PORT=8000

### Статика не загружается

**Проверьте:**
- Файлы скопированы в static/
- В index.html правильные пути: `/static/styles.css`
- app.js использует относительный путь `/api`

### CORS ошибки

**В main.py обновите:**
```python
allow_origins=[
    "https://your-domain.com",
    "https://web.telegram.org"
],
```

### База данных не сохраняется

**Добавьте Volume:**
- Путь: `/app/notifications.db`
- Это сохранит SQLite файл между перезапусками

**Или переключитесь на PostgreSQL** (рекомендуется для продакшена)

---

## 📝 Полезные команды

### Локальное тестирование Docker образа:

```bash
docker build -t notification-api .
docker run -p 8000:8000 notification-api
```

Проверьте: http://localhost:8000

---

## ✨ После успешного деплоя

1. ✅ Протестировать все эндпоинты
2. ✅ Настроить Telegram бота
3. ✅ Интегрировать Airtable webhooks
4. ✅ Добавить мониторинг (опционально)
5. ✅ Настроить резервное копирование БД

---

Готово! Удачи с деплоем! 🚀

