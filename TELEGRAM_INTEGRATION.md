# Интеграция с Telegram Bot

## Процесс подключения

### Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Настройка Mini App

1. Откройте [@BotFather](https://t.me/botfather)
2. Выберите вашего бота
3. Отправьте команду `/newapp`
4. Следуйте инструкциям:
   - Укажите название мини-приложения
   - Загрузите иконку (512x512px)
   - Укажите URL вашего фронтенда (например: `https://your-domain.com/index.html`)
   - Добавьте короткое описание
   - Опционально: добавьте скриншоты и gif

### Шаг 3: Развертывание приложения

#### Вариант А: Хостинг фронтенда отдельно (рекомендуется)

1. **Фронтенд** (HTML/CSS/JS):
   - Загрузите файлы на статический хостинг:
     - [Vercel](https://vercel.com) (бесплатно)
     - [Netlify](https://netlify.com) (бесплатно)
     - [GitHub Pages](https://pages.github.com) (бесплатно)
     - Любой другой статический хостинг

2. **Backend** (FastAPI):
   - Разверните на:
     - [Railway](https://railway.app) (бесплатный тариф)
     - [Render](https://render.com) (бесплатный тариф)
     - [Heroku](https://heroku.com)
     - VPS сервер

3. **Обновите URL в `app.js`:**
   ```javascript
   const API_BASE_URL = 'https://your-backend-domain.com/api';
   ```

#### Вариант Б: Всё на одном домене

Разместите и фронтенд, и API на одном сервере:
- Фронтенд: `https://your-domain.com/`
- API: `https://your-domain.com/api/`

### Шаг 4: Настройка CORS

В `main.py` обновите настройки CORS:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-frontend-domain.com",
        "https://web.telegram.org",  # Telegram WebApp
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Шаг 5: Тестирование

#### Локальное тестирование без бота:

1. Запустите FastAPI:
   ```bash
   python main.py
   ```

2. Откройте `index.html` в браузере
3. API будет доступен на `http://localhost:8000/api`

#### Тестирование с ботом:

1. Разверните приложение на хостинге
2. Убедитесь, что Mini App URL настроен в BotFather
3. Откройте бота в Telegram
4. Нажмите на кнопку "Open Mini App" или отправьте команду `/start` если настроена кнопка

### Шаг 6: Создание кнопки для Mini App

В коде бота (если используете python-telegram-bot):

```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo

# Создание кнопки с Mini App
keyboard = [
    [InlineKeyboardButton(
        "🔔 Уведомления",
        web_app=WebAppInfo(url="https://your-frontend-domain.com")
    )]
]
reply_markup = InlineKeyboardMarkup(keyboard)

# Отправка сообщения с кнопкой
await context.bot.send_message(
    chat_id=update.effective_chat.id,
    text="Нажмите кнопку, чтобы открыть систему уведомлений",
    reply_markup=reply_markup
)
```

Или через BotFather:
1. Отправьте `/mybots` → выберите бота
2. Bot Settings → Menu Button
3. Configure → Add Web App
4. Укажите название и URL вашего Mini App

---

## Архитектура системы

```
┌─────────────────┐
│  Telegram Bot   │
│  (BotFather)    │
└────────┬────────┘
         │
         │ Opens Mini App
         ▼
┌─────────────────┐
│  Mini App       │  ──────────┐
│  (Frontend)     │            │
│  index.html     │            │
└────────┬────────┘            │
         │                     │
         │ API Calls           │ Webhooks
         │                     │
         ▼                     │
┌─────────────────┐            │
│  FastAPI        │◄───────────┘
│  (Backend)      │
│  main.py        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  SQLite DB      │
│  notifications  │
└─────────────────┘
         ▲
         │
         │ Events
┌─────────────────┐
│  Airtable       │
│  (Webhook)      │
└─────────────────┘
```

---

## Безопасность

### 1. Валидация Telegram WebApp

В `app.js` можно добавить проверку данных от Telegram:

```javascript
if (tg && tg.initData) {
    // Валидация initData (требует секретный ключ на бэкенде)
    // Отправка на бэкенд для проверки подлинности
}
```

На бэкенде (FastAPI):

```python
from telegram import Bot
import hmac
import hashlib

async def validate_telegram_user(init_data: str):
    """Валидация данных от Telegram"""
    bot = Bot(token=TELEGRAM_BOT_TOKEN)
    # Валидация init_data...
    return True
```

### 2. Аутентификация

Можно добавить простую аутентификацию через Telegram:

```python
from fastapi import Depends, HTTPException, Header

async def verify_telegram_user(
    x_telegram_init_data: str = Header(None)
):
    """Проверка пользователя через Telegram"""
    if not x_telegram_init_data:
        raise HTTPException(status_code=401, detail="Unauthorized")
    # Валидация...
    return {"user_id": "telegram_user_id"}
```

---

## Тестирование без хостинга

Для локального тестирования можно использовать:

1. **Ngrok** (для публичного URL):
   ```bash
   ngrok http 8000
   ```
   Используйте полученный URL для Mini App в BotFather

2. **Локальная разработка**:
   - Откройте `index.html` напрямую
   - API на `http://localhost:8000`
   - Работает для разработки UI

---

## Рекомендуемый порядок разработки

1. ✅ **Создать и протестировать интерфейс** (уже сделано)
2. ✅ **Создать FastAPI с REST API** (уже сделано)
3. ⏳ **Протестировать API локально** (следующий шаг)
4. ⏳ **Развернуть на хостинге**
5. ⏳ **Настроить Telegram бота и Mini App**
6. ⏳ **Интегрировать с Airtable webhooks**
7. ⏳ **Добавить аутентификацию и безопасность**

---

## Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [FastAPI Documentation](https://fastapi.tiangolo.com)
- [Airtable Webhooks](https://airtable.com/api/webhooks)

