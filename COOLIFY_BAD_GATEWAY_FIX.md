# Исправление Bad Gateway в Coolify

## Проблема:
```
502 Bad Gateway
```

Это означает, что Coolify не может подключиться к вашему приложению внутри контейнера.

---

## Возможные причины и решения:

### 1. Неправильный порт в настройках Coolify

**Проверьте:**
- В настройках приложения в Coolify
- Найдите **"Port"** или **"Port Mapping"**
- Должен быть указан порт **8000**

**Исправьте:**
- Установите порт: `8000`
- Сохраните и перезапустите деплой

---

### 2. Приложение не запускается

**Проверьте логи:**
- В Coolify откройте **"Logs"** вашего приложения
- Ищите ошибки при запуске

**Возможные ошибки:**
- Импорт модулей (если остались старые зависимости)
- Ошибки в main.py
- Проблемы с путями к файлам

---

### 3. Неправильные переменные окружения

**Проверьте переменные в Coolify:**
- В настройках приложения → **"Environment Variables"**
- Убедитесь что нет конфликтующих переменных

**Для упрощенной версии НЕ нужны:**
- `DATABASE_URL` (не нужна БД)
- `HOST` (используется 0.0.0.0 по умолчанию)
- `PORT` (используется 8000 по умолчанию)

Можно оставить все пустым или удалить лишние переменные.

---

### 4. Health Check не проходит

**В Coolify проверьте:**
- Settings → Health Check
- Path должен быть: `/` или `/health`
- Или отключите Health Check временно

---

## Пошаговая проверка:

### Шаг 1: Проверьте логи

1. Откройте приложение в Coolify
2. Перейдите в **"Logs"**
3. Посмотрите что пишет при запуске

**Должно быть что-то вроде:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

**Если есть ошибки** - скопируйте их и исправьте.

---

### Шаг 2: Проверьте настройки порта

1. Settings → **Port**
2. Убедитесь что указано: `8000`
3. Сохраните

---

### Шаг 3: Проверьте переменные окружения

1. Settings → **Environment Variables**
2. Удалите лишние переменные (особенно если есть DATABASE_URL)
3. Оставьте только необходимые

---

### Шаг 4: Перезапустите деплой

1. Нажмите **"Redeploy"** или **"Restart"**
2. Дождитесь завершения
3. Проверьте логи снова

---

## Быстрое исправление:

### Если использовали старую версию с БД:

Убедитесь что в репозитории:
- ✅ `main.py` - упрощенная версия (без БД)
- ✅ `Dockerfile` - упрощенный (без БД зависимостей)
- ✅ Перезапустите деплой

### Если код обновлен но не задеплоен:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Simplified version - static only"
   git push
   ```

2. В Coolify нажмите **"Redeploy"**

---

## Проверка что приложение работает:

После исправления:

1. Проверьте логи - должны быть строки запуска uvicorn
2. Попробуйте открыть URL снова
3. Должна открыться страница с уведомлениями

---

## Альтернатива: Проверка через curl

Если у вас есть доступ к серверу Coolify:

```bash
curl http://localhost:8000
```

Должен вернуть HTML страницу.

---

## Частые ошибки:

### Ошибка: "ModuleNotFoundError"
**Решение:** Убедитесь что в Dockerfile установлен только FastAPI и uvicorn

### Ошибка: "Address already in use"
**Решение:** Проверьте что порт 8000 свободен в контейнере

### Ошибка: "File not found"
**Решение:** Проверьте что файлы static/ скопированы в Docker образ

---

Проверьте логи и сообщите что там написано! 🔍




