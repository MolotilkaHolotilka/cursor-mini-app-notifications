# 🔧 Настройка API для GitHub Pages

После хостинга фронтенда на GitHub Pages, вам нужно настроить подключение к API серверу.

## 📋 Шаги настройки

### 1. Развернуть API сервер

Разверните ваш FastAPI backend на любом хостинге:
- **Coolify** (рекомендуется)
- **Railway**
- **Render**
- **Heroku**
- Любой VPS с Docker

### 2. Настроить CORS

В `main.py` уже добавлены настройки CORS для GitHub Pages:
```python
allowed_origins = [
    "https://molotilkaholotilka.github.io",
    "https://web.telegram.org",
    # ... другие
]
```

Если ваш API на другом домене, добавьте его в `main.py`:
```python
allowed_origins = [
    "https://molotilkaholotilka.github.io",
    "https://web.telegram.org",
    "https://your-api-domain.com",  # Добавьте сюда
]
```

### 3. Настроить API URL во фронтенде

Откройте `app.js` (или `static/app.js`) и найдите строку:

```javascript
return 'https://your-api-domain.com/api';
```

Замените на реальный URL вашего API:

```javascript
return 'https://your-coolify-app.com/api';
```

**Примеры:**
- Coolify: `https://notifications-api.your-domain.com/api`
- Railway: `https://notifications-api.railway.app/api`
- Render: `https://notifications-api.onrender.com/api`

### 4. Альтернативный способ: через переменную окружения

Вы также можете установить API URL через JavaScript перед загрузкой `app.js`:

В `index.html` добавьте перед `<script src="app.js">`:

```html
<script>
    // Установите URL вашего API сервера
    window.API_BASE_URL = 'https://your-api-domain.com/api';
</script>
<script src="app.js"></script>
```

### 5. Проверить работу API

После настройки:

1. Откройте консоль браузера (F12)
2. Откройте ваше приложение на GitHub Pages
3. Проверьте наличие запросов к API в Network вкладке
4. Убедитесь, что нет CORS ошибок

### 6. Тестирование локально

Для локальной разработки:

1. Запустите API: `python main.py` (на `http://localhost:8000`)
2. Откройте `index.html` в браузере
3. API автоматически определится как `http://localhost:8000/api`

## 🐛 Решение проблем

### CORS ошибки

Если видите ошибку типа:
```
Access to fetch at 'https://api-domain.com/api/...' from origin 'https://molotilkaholotilka.github.io' has been blocked by CORS policy
```

**Решение:**
1. Убедитесь, что ваш домен GitHub Pages добавлен в `allowed_origins` в `main.py`
2. Перезапустите API сервер
3. Проверьте, что API доступен по HTTPS (GitHub Pages работает только с HTTPS)

### API не отвечает

1. Проверьте, что API сервер запущен
2. Проверьте URL API в `app.js`
3. Откройте URL API в браузере: `https://your-api-domain.com/health`
4. Проверьте логи API сервера

### Fallback на моки

Если API недоступен, приложение автоматически переключится на моки данных. Это временное решение для разработки. В продакшене убедитесь, что API всегда доступен.

## 📝 Структура API

API должен предоставлять следующие эндпоинты:

- `GET /api/notifications` - список уведомлений
- `GET /api/stats` - статистика
- `POST /api/notifications/{id}/read` - отметить как прочитанное

Подробнее см. `API_DOCUMENTATION.md`

## ✅ Чеклист

- [ ] API сервер развернут и доступен
- [ ] CORS настроен в `main.py`
- [ ] API URL указан в `app.js`
- [ ] API отвечает на `/health`
- [ ] Нет CORS ошибок в консоли
- [ ] Уведомления загружаются с API

Готово! 🚀




