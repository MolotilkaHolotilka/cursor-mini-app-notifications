# Подключение к GitHub через Cursor/VS Code

## Способ 1: Через встроенное расширение (РЕКОМЕНДУЕТСЯ) ✨

### Шаг 1: Создать репозиторий на GitHub

1. Зайдите на https://github.com/new
2. Создайте новый репозиторий:
   - **Repository name:** `telegram-notification-system`
   - Оставьте **пустым** (не добавляйте README, .gitignore, лицензию)
   - Выберите Public или Private
3. Нажмите **"Create repository"**

### Шаг 2: Публикация через Cursor/VS Code

1. Откройте панель **Source Control** (иконка ветки слева или `Ctrl+Shift+G`)
2. Нажмите на кнопку **"Publish to GitHub"** или **"..."** → **"Publish Branch"**
3. Выберите:
   - **Public** или **Private**
   - Название репозитория
4. Авторизуйтесь через встроенную систему (если нужно)
5. Готово! Код автоматически загрузится на GitHub

---

## Способ 2: Через терминал (вручную)

### Если репозиторий уже создан на GitHub:

1. Скопируйте URL вашего репозитория (например: `https://github.com/ваш-username/telegram-notification-system.git`)

2. Выполните команды:

```bash
# Подключить удалённый репозиторий
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Или если remote уже есть, обновить URL
git remote set-url origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Проверить подключение
git remote -v

# Отправить код
git push -u origin main
```

### Если нужно авторизоваться:

GitHub больше не принимает пароли. Используйте:

**Вариант А: Personal Access Token**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo`
4. Используйте токен вместо пароля при `git push`

**Вариант Б: GitHub CLI**
```bash
gh auth login
```

---

## Способ 3: Через скрипт

Запустите `connect_github.bat` и введите URL репозитория:

```bash
connect_github.bat
```

---

## Проверка после подключения

1. Зайдите на ваш GitHub профиль
2. Найдите созданный репозиторий
3. Убедитесь что все файлы загружены:
   - ✅ Dockerfile
   - ✅ main.py
   - ✅ static/
   - ✅ requirements.txt
   - ✅ README.md
   - И т.д.

---

## Что делать дальше

После успешной загрузки на GitHub:

1. ✅ Проверить что все файлы на месте
2. ✅ Настроить деплой на Coolify:
   - В Coolify выберите "Git Repository"
   - Подключите ваш GitHub репозиторий
   - Coolify автоматически найдет Dockerfile
   - Задеплойте!

---

## Troubleshooting

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin YOUR_URL
```

### Ошибка авторизации
- Используйте Personal Access Token
- Или авторизуйтесь через расширение VS Code/Cursor

### Ошибка: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

Готово! 🚀

